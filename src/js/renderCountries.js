// import { refs } from './refs';
// export default renderCountries;

// function createCountry({ name }) {
//   return `<li>${name}</li>`;
// }

// function createListOfCountries(countries) {
//   return countries.map(createCountry).join('');
// }

// function renderCountries(countries) {
//   refs.list.insertAdjacentHTML('beforeend', createListOfCountries(countries));
// }
